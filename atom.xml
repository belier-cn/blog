<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://blog.belier.cn</id>
    <title>Belier Blog</title>
    <updated>2020-05-10T08:30:37.310Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://blog.belier.cn"/>
    <link rel="self" href="https://blog.belier.cn/atom.xml"/>
    <logo>https://blog.belier.cn/images/avatar.png</logo>
    <icon>https://blog.belier.cn/favicon.ico</icon>
    <rights>All rights reserved 2020, Belier Blog</rights>
    <entry>
        <title type="html"><![CDATA[将 Maven 项目发布到 Nexus 私有仓库]]></title>
        <id>https://blog.belier.cn/post/86123957/</id>
        <link href="https://blog.belier.cn/post/86123957/">
        </link>
        <updated>2020-05-10T06:12:02.000Z</updated>
        <content type="html"><![CDATA[<h2 id="安装-nexus-私有仓库">安装 Nexus 私有仓库</h2>
<p>参考：<a href="/post/63571659/">Docker Compose 运行 nexus3</a></p>
<h2 id="修改-maven-项目的-pomxml">修改 Maven 项目的 pom.xml</h2>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;project xmlns=&quot;http://maven.apache.org/POM/4.0.0&quot;
         xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;
         xsi:schemaLocation=&quot;http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd&quot;&gt;
    &lt;distributionManagement&gt;
        &lt;snapshotRepository&gt;
            &lt;id&gt;snapshots&lt;/id&gt;
            &lt;!-- 替换成你的地址 --&gt;
            &lt;url&gt;http://192.168.56.10:8081/repository/maven-snapshots/&lt;/url&gt;
        &lt;/snapshotRepository&gt;
        &lt;repository&gt;
            &lt;id&gt;releases&lt;/id&gt;
            &lt;!-- 替换成你的地址 --&gt;
            &lt;url&gt;http://192.168.56.10:8081/repository/maven-releases/&lt;/url&gt;
        &lt;/repository&gt;
    &lt;/distributionManagement&gt;
&lt;/project&gt;
</code></pre>
<h2 id="修改-maven-配置文件-settingsxml">修改 Maven 配置文件 settings.xml</h2>
<pre><code class="language-xml">&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;
&lt;settings xmlns=&quot;http://maven.apache.org/SETTINGS/1.0.0&quot;
          xmlns:xsi=&quot;http://www.w3.org/2001/XMLSchema-instance&quot;
          xsi:schemaLocation=&quot;http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd&quot;&gt;

    &lt;servers&gt;
        &lt;server&gt;
            &lt;id&gt;releases&lt;/id&gt;
            &lt;username&gt;admin&lt;/username&gt;
            &lt;password&gt;{your_password}&lt;/password&gt;
        &lt;/server&gt;
        &lt;server&gt;
            &lt;id&gt;snapshots&lt;/id&gt;
            &lt;username&gt;admin&lt;/username&gt;
            &lt;password&gt;{your_password}&lt;/password&gt;
        &lt;/server&gt;
    &lt;/servers&gt;
&lt;/settings&gt;
</code></pre>
<h2 id="发布">发布</h2>
<p>如果使用 IDEA，直接在 IDEA 右侧的 Maven 小窗口中执行 deploy</p>
<p>命令方式：在项目根路径下执行如下命令</p>
<pre><code class="language-bash"># --settings E:\maven\settings.xml 指定配置文件，以自己的路径为准
# -Dmaven.test.skip=true 跳过测试，看自己需求
mvn deploy --settings E:\maven\settings.xml -Dmaven.test.skip=true
</code></pre>
<h2 id="使用私有仓库">使用私有仓库</h2>
<p>参考：<a href="/post/66712598/">Maven 同时配置阿里云仓库和私有仓库</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Docker Compose 运行 nexus3]]></title>
        <id>https://blog.belier.cn/post/63571659/</id>
        <link href="https://blog.belier.cn/post/63571659/">
        </link>
        <updated>2020-05-10T05:08:52.000Z</updated>
        <content type="html"><![CDATA[<h2 id="创建目录">创建目录</h2>
<pre><code class="language-bash">mkdir -p /usr/local/src/nexus3/data
</code></pre>
<h2 id="赋予-data-目录权限">赋予 data 目录权限</h2>
<pre><code class="language-bash">chown -R 200 /usr/local/src/nexus3/data
</code></pre>
<h2 id="创建-docker-composeyml-文件">创建 docker-compose.yml 文件</h2>
<pre><code class="language-yml">version: '3'
services:
  nexus3:
    image: sonatype/nexus3
    restart: always
    container_name: nexus
    ports:
      - 8081:8081
    volumes:
      - $PWD/data:/nexus-data
</code></pre>
<h2 id="启动">启动</h2>
<pre><code class="language-bash">docker-compse up -d
</code></pre>
<p>启动比较慢，大概2-5分钟</p>
<h2 id="用户名和密码">用户名和密码</h2>
<p>默认用户名：admin<br>
密码：/usr/local/src/nexus3/data/admin.password 文件的内容就是密码</p>
<h2 id="参考">参考</h2>
<p><a href="https://hub.docker.com/r/sonatype/nexus3">docker hub nexus3</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Spring Boot Swagger Pageable 处理]]></title>
        <id>https://blog.belier.cn/post/52178956/</id>
        <link href="https://blog.belier.cn/post/52178956/">
        </link>
        <updated>2020-03-01T09:04:28.000Z</updated>
        <content type="html"><![CDATA[<h2 id="将-pageable-当成-void-处理">将 Pageable 当成 Void 处理</h2>
<pre><code class="language-java">@Bean
public AlternateTypeRuleConvention pageableConvention(final TypeResolver resolver) {
    return new AlternateTypeRuleConvention() {
        @Override
        public int getOrder() {
            return Ordered.HIGHEST_PRECEDENCE;
        }

        @Override
        public List&lt;AlternateTypeRule&gt; rules() {
            // 将 Pageable 当成 Void 处理，配合 PageableParamPlugin 进行处理
            return Lists.newArrayList(AlternateTypeRules.newRule(resolver.resolve(Pageable.class), resolver.resolve(Void.class)));
        }
    };
}
</code></pre>
<h2 id="在参数中有-pageable-的接口中手动添加分页参数">在参数中有 Pageable 的接口中手动添加分页参数</h2>
<pre><code class="language-java">@Component
@Order(Ordered.HIGHEST_PRECEDENCE)
public class PageableParamPlugin implements OperationBuilderPlugin {

    private ResolvedType pageableType;

    private List&lt;Parameter&gt; parameters;

    private final TypeResolver resolver;

    private final SpringDataWebProperties springDataWebProperties;

    public PageableParamPlugin(TypeResolver resolver, SpringDataWebProperties springDataWebProperties) {
        this.resolver = resolver;
        this.springDataWebProperties = springDataWebProperties;
    }

    @PostConstruct
    public void init() {
        if (resolver == null || springDataWebProperties == null) {
            return;
        }
        this.pageableType = resolver.resolve(Pageable.class);
        ModelRef stringModel = new ModelRef(&quot;string&quot;);
        ModelRef intModel = new ModelRef(&quot;int&quot;);
        String parameterType = &quot;query&quot;;
        parameters = Lists.newArrayListWithExpectedSize(3);
        parameters.add(new ParameterBuilder()
                .parameterType(parameterType)
                .name(springDataWebProperties.getPageable().getPageParameter())
                .modelRef(intModel)
                .description(&quot;当前页&quot;)
                .build());
        parameters.add(new ParameterBuilder()
                .parameterType(parameterType)
                .name(springDataWebProperties.getPageable().getSizeParameter())
                .modelRef(intModel)
                .description(&quot;查询的记录数&quot;)
                .build());
        parameters.add(new ParameterBuilder()
                .parameterType(parameterType)
                .name(springDataWebProperties.getSort().getSortParameter())
                .modelRef(stringModel)
                .allowMultiple(true)
                .description(&quot;排序参数&quot;)
                .build());
    }

    @Override
    public void apply(OperationContext context) {
        if (CollectionUtils.isEmpty(parameters)) {
            return;
        }
        boolean hasPageableParam = context.getParameters()
                .stream().anyMatch(parameter -&gt; pageableType.equals(parameter.getParameterType()));
        if (hasPageableParam) {
            context.operationBuilder().parameters(parameters);
        }
    }


    @Override
    public boolean supports(DocumentationType delimiter) {
        return true;
    }

}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Spring Boot Swagger 枚举处理]]></title>
        <id>https://blog.belier.cn/post/35678129/</id>
        <link href="https://blog.belier.cn/post/35678129/">
        </link>
        <updated>2020-02-29T06:23:36.000Z</updated>
        <content type="html"><![CDATA[<h2 id="创建-swaggerenum-注解">创建 SwaggerEnum 注解</h2>
<p>用于标识枚举类</p>
<pre><code class="language-java">@Target({ElementType.TYPE})
@Retention(RetentionPolicy.RUNTIME)
public @interface SwaggerEnum {

    String VALUE = &quot;value&quot;;

    String DESC = &quot;desc&quot;;

    /**
     * @return 枚举的含义
     */
    String value();

    /**
     * @return 数据库实际对应的类型
     */
    Class type() default Integer.class;

    /**
     * @return 数据库实际对应的类型
     */
    String modelRef() default &quot;int&quot;;

    /**
     * @return 枚举实际使用的字段
     */
    String valueName() default VALUE;

    /**
     * @return 枚举中用于描述的字段
     */
    String descName() default DESC;

}

</code></pre>
<h2 id="创建处理枚举的工具类">创建处理枚举的工具类</h2>
<pre><code class="language-java">@Component
public class EnumCommon {

    /**
     * 获取 {@link SwaggerEnum}
     *
     * @param enumClass 枚举类
     * @return {@link SwaggerEnum}
     */
    public SwaggerEnum getSwaggerEnum(Class enumClass) {
        if (enumClass == null || !Enum.class.isAssignableFrom(enumClass)) {
            return null;
        }
        return AnnotationUtils.findAnnotation(enumClass, SwaggerEnum.class);
    }

    /**
     * 生成 desc
     *
     * @param enumClass   枚举类
     * @param swaggerEnum {@link SwaggerEnum}
     * @return desc
     */
    public String getDesc(Class enumClass, SwaggerEnum swaggerEnum) {
        if (swaggerEnum == null) {
            return null;
        }
        // 获取枚举对应的值
        Object[] enumConstants = enumClass.getEnumConstants();
        if (ArrayUtils.isEmpty(enumConstants)) {
            return null;
        }
        // 将枚举的值转成字符串
        return Arrays.stream(enumConstants)
                .filter(Objects::nonNull)
                .map(item -&gt; {
                    try {
                        Reflect enumReflect = Reflect.on(item);
                        Object value = enumReflect.get(swaggerEnum.valueName());
                        Object desc = enumReflect.get(swaggerEnum.descName());
                        return StrUtil.format(&quot;{} : {}&quot;, value, desc);
                    } catch (Exception e) {
                        return null;
                    }
                }).filter(Objects::nonNull)
                .collect(Collectors.joining(&quot;，&quot;, swaggerEnum.value() + &quot;  &quot;, &quot;&quot;));
    }


}
</code></pre>
<p>Reflect 使用的 <a href="https://github.com/jOOQ/jOOR">joor</a><br>
StrUtil 使用的 <a href="https://hutool.cn/">hutool</a></p>
<h2 id="创建-expandedparameterbuilderplugin">创建 ExpandedParameterBuilderPlugin</h2>
<p>用于处理 query 传参中的枚举</p>
<pre><code class="language-java">@Component
public class EnumExpandedParameterBuilderPlugin implements ExpandedParameterBuilderPlugin {

    private final EnumCommon enumCommon;

    private final TypeResolver resolver;

    public EnumExpandedParameterBuilderPlugin(EnumCommon enumCommon, TypeResolver resolver) {
        this.enumCommon = enumCommon;
        this.resolver = resolver;
    }

    @Override
    public void apply(ParameterExpansionContext context) {
        Class&lt;?&gt; erasedType = context.getFieldType().getErasedType();
        SwaggerEnum swaggerEnum = enumCommon.getSwaggerEnum(erasedType);
        String desc = enumCommon.getDesc(erasedType, swaggerEnum);
        if (desc != null) {
            Reflect parameterBuilderReflect = Reflect.on(context.getParameterBuilder());
            // 将 allowableValues 设置成 null
            // context.getParameterBuilder().allowableValues() 并不能设置 null，只能反射
            parameterBuilderReflect.set(&quot;allowableValues&quot;, null);
            context.getParameterBuilder()
                    .allowMultiple(false)
                    .modelRef(new ModelRef(swaggerEnum.modelRef()))
                    .description(desc)
                    .type(resolver.resolve(swaggerEnum.type()));
        }

    }

    @Override
    public boolean supports(DocumentationType delimiter) {
        return true;
    }

}
</code></pre>
<h2 id="创建-modelpropertybuilderplugin">创建 ModelPropertyBuilderPlugin</h2>
<p>用于处理 request body 对应的类以及接口的返回值的类</p>
<pre><code class="language-java">@Component
public class EnumModelPropertyBuilderPlugin implements ModelPropertyBuilderPlugin {

    private final EnumCommon enumCommon;

    public EnumModelPropertyBuilderPlugin(EnumCommon enumCommon) {
        this.enumCommon = enumCommon;
    }

    @Override
    public void apply(ModelPropertyContext context) {
        if (context.getBeanPropertyDefinition().isPresent()) {
            Class&lt;?&gt; rawPrimaryType = context.getBeanPropertyDefinition().get().getRawPrimaryType();
            SwaggerEnum swaggerEnum = enumCommon.getSwaggerEnum(rawPrimaryType);
            String desc = enumCommon.getDesc(rawPrimaryType, swaggerEnum);
            if (desc != null) {
                // 修改描述和类型
                ResolvedType type = context.getResolver().resolve(swaggerEnum.type());
                context.getBuilder().description(desc).type(type);
            }
        }
    }

    @Override
    public boolean supports(DocumentationType documentationType) {
        return true;
    }

}
</code></pre>
<h2 id="示例">示例</h2>
<pre><code class="language-java">@SwaggerEnum(&quot;性别&quot;)
public enum GenderEnum {

    /**
     * 枚举值
     */
    MAN(1, &quot;男&quot;),
    WOMAN(2, &quot;女&quot;);

    @Getter
    private int value;

    @Getter
    private String desc;

    GenderEnum(int value, String desc) {
        this.value = value;
        this.desc = desc;
    }

}
</code></pre>
<p>最后生成的 description ：<code>性别 1 : 男，2 : 女</code></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Spring Boot 优雅的使用枚举类]]></title>
        <id>https://blog.belier.cn/post/61782395/</id>
        <link href="https://blog.belier.cn/post/61782395/">
        </link>
        <updated>2020-02-29T05:58:44.000Z</updated>
        <content type="html"><![CDATA[<h2 id="枚举规范接口类">枚举规范接口类</h2>
<p>所有的枚举类都实现该接口，统一规范</p>
<pre><code class="language-java">public interface BaseEnum {

    /**
     * 获取实际的值
     * Jackson 在反序列化的时候会根据 value 进行反序列化，所以不用自定义反序列化规则
     *
     * @return value
     */
    int getValue();

    /**
     * 获取对应的描述
     *
     * @return desc
     */
    String getDesc();
}
</code></pre>
<h2 id="jackson-序列化">Jackson 序列化</h2>
<p>创建序列化类</p>
<pre><code class="language-java">public class BaseEnumSerializer extends JsonSerializer&lt;BaseEnum&gt; {

    /**
     * 缓存
     */
    private static final Map&lt;BaseEnum, Map&lt;String, Object&gt;&gt; CACHE = new HashMap&lt;&gt;();

    @Override
    public void serialize(BaseEnum baseEnum, JsonGenerator gen, SerializerProvider serializers) throws IOException {
        if (baseEnum == null) {
            gen.writeObject(MapUtils.EMPTY_SORTED_MAP);
            return;
        }
        Map&lt;String, Object&gt; result = Optional.ofNullable(CACHE.get(baseEnum)).orElseGet(() -&gt; {
            HashMap&lt;String, Object&gt; map = new HashMap&lt;&gt;(2);
            map.put(&quot;value&quot;, baseEnum.getValue());
            map.put(&quot;desc&quot;, baseEnum.getDesc());
            CACHE.put(baseEnum, map);
            return map;
        });
        gen.writeObject(result);
    }

    @Override
    public Class&lt;BaseEnum&gt; handledType() {
        return BaseEnum.class;
    }
}
</code></pre>
<p>Spring Boot Jackson 配置</p>
<pre><code class="language-java">@Configuration
public class JacksonConfig {

    @Bean
    public Jackson2ObjectMapperBuilderCustomizer objectMapperBuilderCustomizer() {
        return objectMapperBuilder -&gt; objectMapperBuilder
                .serializers(new BaseEnumSerializer());
    }

}
</code></pre>
<h2 id="jackson-反序列化">Jackson 反序列化</h2>
<p>在枚举类中定义一个静态方法用于返回枚举值，并使用<code>JsonCreator</code>注解标识<br>
示例：</p>
<pre><code class="language-java">@JsonCreator
public static BaseEnum getItem(String value){
    // 根据 value 返回具体的枚举值
    return null;
}
</code></pre>
<h2 id="spring-mvc-使用枚举传参">Spring MVC 使用枚举传参</h2>
<p>创建 ConverterFactory 和 Convert</p>
<pre><code class="language-java">public class StringToEnumConverterFactory implements ConverterFactory&lt;String, BaseEnum&gt; {

    @Override
    public &lt;T extends BaseEnum&gt; Converter&lt;String, T&gt; getConverter(Class&lt;T&gt; targetType) {
        return new StringToEnumConverter&lt;&gt;(targetType);
    }


    private static class StringToEnumConverter&lt;T extends BaseEnum&gt; implements Converter&lt;String, T&gt; {

        private Map&lt;String, T&gt; enumMap = Maps.newHashMap();

        public StringToEnumConverter(Class&lt;T&gt; enumType) {
            T[] enums = enumType.getEnumConstants();
            for (T e : enums) {
                enumMap.put(e.getValue() + &quot;&quot;, e);
            }
        }

        @Override
        public T convert(String source) {
            T t = enumMap.get(source);
            if (ObjectUtil.isNull(t)) {
                throw new IllegalArgumentException(&quot;无法匹配对应的枚举值&quot;);
            }
            return t;
        }
    }

}
</code></pre>
<h2 id="mybatis-plus-使用枚举">Mybatis Plus 使用枚举</h2>
<p><a href="https://mybatis.plus/guide/enum.html">https://mybatis.plus/guide/enum.html</a></p>
<h2 id="通用-mapper-使用枚举">通用 Mapper 使用枚举</h2>
<p><a href="https://github.com/abel533/Mapper/wiki/7.2.typehandler">https://github.com/abel533/Mapper/wiki/7.2.typehandler</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Maven 项目 Jenkins Pipeline 使用 Sonarqube]]></title>
        <id>https://blog.belier.cn/post/38512795/</id>
        <link href="https://blog.belier.cn/post/38512795/">
        </link>
        <updated>2020-02-17T12:23:17.000Z</updated>
        <content type="html"><![CDATA[<h2 id="环境安装">环境安装</h2>
<p><a href="/post/58129673/">使用 Docker Compose 运行 SonarQube 7.9.2</a><br>
<a href="/post/55817706/">使用 Docker Compose 运行 Jenkins</a></p>
<h2 id="sonarqube-创建令牌">Sonarqube 创建令牌</h2>
<p>创建地址：<code>{your_sonarqube_server}/account/security/</code><br>
填写令牌名称，然后创建，令牌记得立即保存，否则后面就看不到了</p>
<h2 id="sonarqube-配置-jenkins-webhooks">Sonarqube 配置 Jenkins Webhooks</h2>
<p>配置地址：<code>{your_sonarqube_server}/admin/webhooks</code><br>
Jenkins Webhooks 地址：<code>{your_jenkins_server}/sonarqube-webhook/</code></p>
<h2 id="jenkins-安装-sonarqube-插件">Jenkins 安装 Sonarqube 插件</h2>
<p>插件列表搜索关键词 <code>SonarQube Scanner for Jenkins</code> 进行安装</p>
<h2 id="jenkins-配置-sonarqube-server">Jenkins 配置 Sonarqube Server</h2>
<h3 id="创建凭证">创建凭证</h3>
<p>创建地址：<code>{your_jenkins_server}/credentials/store/system/domain/_/newCredentials</code><br>
选择 Sercert text 类型，Sercert 填写 <code>刚创建的 Sonarqube 令牌</code>，ID 随便写（如：sonarqube）</p>
<h3 id="配置-server">配置 Server</h3>
<p>配置地址：<code>{your_jenkins_server}/configure</code><br>
添加一个 Sonarqube Server，填写 Name（后面 withSonarQubeEnv 使用的就是这个名称）、Server Url ，选择刚才创建的凭证，然后保存即可</p>
<h2 id="jenkins-pipeline-使用-sonarqube">Jenkins Pipeline 使用 Sonarqube</h2>
<p>以 Maven 项目为例</p>
<pre><code class="language-groovy">pipeline {
    agent none
    // 定义环境变量
    environment {
        GIT_URL = 'https://github.com/belier-cn/spring-boot-docker.git'
    }
    stages {
        stage('Checkout') {
            agent any
            steps {
                // 拉取代码，参考文档：https://jenkins.io/doc/pipeline/steps/workflow-scm-step/
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], userRemoteConfigs: [[url: GIT_URL]]])
            }
        }
        stage('Build') {
            agent {
                // 参考文档：https://jenkins.io/zh/doc/book/pipeline/docker/
                docker {
                    image 'maven:3-alpine'
                    // 挂载目录，如果要配置 maven 的阿里云仓库地址，在 /usr/local/src/maven 下放一个配置好的 settings.xml 文件即可
                    args '-v /usr/local/src/maven:/root/.m2'
                }
            }
            steps {
                withSonarQubeEnv('SonarQube') {
                    sh 'mvn clean -Dmaven.test.skip=true package org.sonarsource.scanner.maven:sonar-maven-plugin:3.6.0.1398:sonar'
                }
            }

        }

        // 等待 SonarQube 执行结束并回调
        stage(&quot;Quality Gate&quot;) {
            steps {
                // 设置超时时间为1小时，避免一直等待
                timeout(time: 1, unit: 'HOURS') {
                    waitForQualityGate abortPipeline: true
                }
            }
        }

    }
}
</code></pre>
<h2 id="参考">参考</h2>
<p><a href="https://jenkins.io/doc/pipeline/steps/sonar/">SonarQube Scanner for Jenkins</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[使用 Docker Compose 运行 SonarQube 7.9.2]]></title>
        <id>https://blog.belier.cn/post/58129673/</id>
        <link href="https://blog.belier.cn/post/58129673/">
        </link>
        <updated>2020-02-15T04:23:05.000Z</updated>
        <content type="html"><![CDATA[<h2 id="创建目录">创建目录</h2>
<pre><code class="language-bash">mkdir -p /usr/local/src/sonarqube/conf
mkdir -p /usr/local/src/sonarqube/extensions
mkdir -p /usr/local/src/sonarqube/logs
mkdir -p /usr/local/src/sonarqube/data
</code></pre>
<h2 id="赋予目录权限">赋予目录权限</h2>
<pre><code class="language-bash">cd /usr/local/src/sonarqube
chown -R 999:999 .
</code></pre>
<p>否则会出现如下错误</p>
<pre><code>Unable to locate appender &quot;file_es&quot; for logger config &quot;root&quot;
</code></pre>
<p>参考：<a href="https://mlog.club/article/3945654">https://mlog.club/article/3945654</a></p>
<h2 id="设置系统参数">设置系统参数</h2>
<h3 id="临时生效">临时生效</h3>
<pre><code class="language-bash">sysctl -w vm.max_map_count=262144
sysctl -w fs.file-max=65536
ulimit -n 65536
ulimit -u 4096
</code></pre>
<h3 id="ulimit-永久生效">ulimit 永久生效</h3>
<p>编辑 <code>/etc/security/limits.conf</code> 加入如下配置</p>
<pre><code># * 代表所有用户，可以针对某个用户配置 如：root soft notfile 65536
* soft nofile 65536
* hard nofile 65536

* soft nproc 65565
* hard nproc 65565
</code></pre>
<p>参考：<a href="https://github.com/iwhile/blogbook/issues/28">https://github.com/iwhile/blogbook/issues/28</a></p>
<h3 id="sysctl-永久生效">sysctl 永久生效</h3>
<p>编辑 <code>/etc/sysctl.conf</code> 加入如下配置</p>
<pre><code>vm.max_map_count=262144
fs.file-max=65536
</code></pre>
<p>加载 <code>/etc/sysctl.conf</code>配置</p>
<pre><code>sysctl -p
</code></pre>
<h3 id="重启系统">重启系统</h3>
<p>如果采用永久配置，保险起见，重启系统</p>
<h2 id="创建-docker-composeyml-文件">创建 docker-compose.yml 文件</h2>
<p>在<code>/usr/local/src/sonarqube</code>目录下创建</p>
<pre><code class="language-yaml">version: '3'
services:
  sonarqube:
    image: sonarqube:7.9.2-community
    restart: always
    container_name: sonarqube
    ports:
      - 9000:9000
    environment:
      &quot;sonar.jdbc.username&quot;: postgres
      &quot;sonar.dbc.password&quot;: admin123
      &quot;sonar.jdbc.url&quot;: jdbc:postgresql://postgres:5432/sonarqube
    volumes:
      - $PWD/conf:/opt/sonarqube/conf
      - $PWD/extensions:/opt/sonarqube/extensions
      - $PWD/logs:/opt/sonarqube/logs
      - $PWD/data:/opt/sonarqube/data
</code></pre>
<h2 id="启动">启动</h2>
<pre><code class="language-bash">docker-compse up -d
</code></pre>
<h2 id="参考">参考</h2>
<p><a href="https://hub.docker.com/_/sonarqube/">Docker SonarQube</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[使用 Docker Compose 运行 PostgreSQL]]></title>
        <id>https://blog.belier.cn/post/35127562/</id>
        <link href="https://blog.belier.cn/post/35127562/">
        </link>
        <updated>2020-02-15T03:26:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="创建所需目录和文件">创建所需目录和文件</h2>
<pre><code class="language-bash">mkdir -p /usr/local/src/postgres/data
touch /usr/local/src/postgres/postgres.conf
</code></pre>
<h2 id="创建-docker-composeyml-文件">创建 docker-compose.yml 文件</h2>
<p>在<code>/usr/local/src/sonarqube</code>目录下创建</p>
<pre><code class="language-yaml">version: '3'
services:
  postgres:
    image: postgres
    restart: always
    container_name: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: admin123
    volumes:
      - $PWD/postgres.conf:/etc/postgresql/postgresql.conf
      - $PWD/data:/var/lib/postgresql/data
</code></pre>
<h2 id="启动">启动</h2>
<pre><code class="language-bash">docker-compose up -d
</code></pre>
<h2 id="参考">参考</h2>
<p><a href="https://hub.docker.com/_/postgres">Docker PostgreSQL</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[使用 Docker Compose 运行 Mysql 5.7]]></title>
        <id>https://blog.belier.cn/post/69821753/</id>
        <link href="https://blog.belier.cn/post/69821753/">
        </link>
        <updated>2020-02-15T03:17:23.000Z</updated>
        <content type="html"><![CDATA[<h2 id="创建存放目录">创建存放目录</h2>
<pre><code class="language-bash"># 存放数据目录
mkdir -p /usr/local/src/mysql/data
# 存放配置的目录，这里的配置项会覆盖默认的配置项
mkdir -p /usr/local/src/mysql/conf.d
</code></pre>
<h2 id="创建-docker-composeyml-文件">创建 docker-compose.yml 文件</h2>
<p>在 <code>/usr/local/src/mysql</code> 目录下创建</p>
<pre><code class="language-yaml">version: '3'
services:
  mysql:
    image: mysql:5.7
    restart: always
    container_name: mysql
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: admin123
    volumes:
      - $PWD/data:/var/lib/mysql
      - $PWD/conf.d:/etc/mysql/conf.d
    command:
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_general_ci
</code></pre>
<h2 id="启动">启动</h2>
<pre><code class="language-bash">docker-compose up -d
</code></pre>
<h2 id="参考">参考</h2>
<p><a href="https://hub.docker.com/_/mysql">Docker Mysql 官方支持</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Spring Boot Jenkins Pipeline SonarQube Docker]]></title>
        <id>https://blog.belier.cn/post/52168793/</id>
        <link href="https://blog.belier.cn/post/52168793/">
        </link>
        <updated>2020-02-14T02:16:38.000Z</updated>
        <content type="html"><![CDATA[<h2 id="准备一个简单的-spring-boot-项目">准备一个简单的 Spring Boot 项目</h2>
<p>是不是 Spring Boot 项目无所谓，只要是使用 Maven 的项目都可以完全按照来，其它项目除了编译都几乎一样<br>
<a href="https://github.com/belier-cn/spring-boot-docker">https://github.com/belier-cn/spring-boot-docker.git</a></p>
<h2 id="jenkins-安装">Jenkins 安装</h2>
<p><a href="/post/55817706/">使用 Docker Compose 运行 Jenkins</a><br>
不用安装默认推荐插件，太多了，没有必要<br>
<a href="/post/95619377/">Jenkins 汉化和加速插件下载</a></p>
<h2 id="jenkins-安装所需插件">Jenkins 安装所需插件</h2>
<ol>
<li>Pipeline<br>
<a href="https://jenkins.io/zh/doc/book/pipeline/">Pipeline 介绍</a></li>
<li>Blue Ocean<br>
<a href="https://jenkins.io/zh/doc/book/blueocean/">Blue Ocean 介绍</a></li>
<li>Git Plugin<br>
<a href="https://github.com/jenkinsci/git-plugin">Git Plugin Github 地址</a></li>
<li>SSH Pipeline Steps<br>
<a href="https://github.com/jenkinsci/ssh-steps-plugin">SSH Pipeline Steps Github 地址</a></li>
</ol>
<h2 id="创建-pipeline-项目">创建 Pipeline 项目</h2>
<p><a href="https://jenkins.io/zh/doc/book/pipeline/getting-started/">Pipline 入门</a></p>
<h2 id="编写-pipeline-脚本">编写 Pipeline 脚本</h2>
<p><a href="https://jenkins.io/zh/doc/book/pipeline/jenkinsfile/">Jenkinsfile 使用</a></p>
<pre><code class="language-groovy">pipeline {
    agent none
    // 定义环境变量
    environment {
        // 根据 id 获取配置在 Jenkins 中的凭证 参考文档：https://jenkins.io/zh/doc/book/pipeline/jenkinsfile/#%E5%A4%84%E7%90%86%E5%87%AD%E6%8D%AE
        DOCKER_REGISTRY = credentials('aliyun-docker-registry')
        REGISTRY_URL = 'registry.cn-shanghai.aliyuncs.com'
        IMAGE_NAME = 'registry.cn-shanghai.aliyuncs.com/belier/test'
        CONTAINER_NAME = 'test'
        REMOTE_HOST = '192.168.56.11'
        GIT_URL = 'https://github.com/belier-cn/spring-boot-docker.git'
    }
    stages {
        stage('Checkout') {
            agent any
            steps {
                // 拉取代码，参考文档：https://jenkins.io/doc/pipeline/steps/workflow-scm-step/
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], userRemoteConfigs: [[url: GIT_URL]]])
            }
        }
        stage('Build Jar') {
            agent {
                // 参考文档：https://jenkins.io/zh/doc/book/pipeline/docker/
                docker {
                    image 'maven:3-alpine'
                    // 挂载目录，如果要配置 maven 的阿里云仓库地址，在 /usr/local/src/maven 下放一个配置好的 settings.xml 文件即可
                    args '-v /usr/local/src/maven:/root/.m2'
                }
            }
            steps {
                // 打包
                sh 'mvn clean -Dmaven.test.skip=true package'
            }
        }
        stage('Build Image') {
            agent any
            steps {
                // 构建镜像
                sh &quot;docker build -t ${IMAGE_NAME} .&quot;
            }
        }
        stage('Push Image') {
            agent any
            steps {
                // 登陆到阿里云镜像仓库
                sh &quot;docker login -u=${DOCKER_REGISTRY_USR} -p=${DOCKER_REGISTRY_PSW} ${REGISTRY_URL}&quot;
                // 推送镜像到阿里云镜像仓库
                sh &quot;docker push ${IMAGE_NAME}&quot;
            }
        }
        stage('Deploy') {
            agent any
            steps {
                script {
                    // 登陆远程服务器，拉取镜像并运行，参考文档：https://github.com/jenkinsci/ssh-steps-plugin#withcredentials
                    def remote = [:]
                    remote.name = &quot;centos7-11&quot;
                    remote.host = REMOTE_HOST
                    remote.allowAnyHosts = true
                    withCredentials([sshUserPrivateKey(credentialsId: REMOTE_HOST, keyFileVariable: 'identity', passphraseVariable: '', usernameVariable: 'userName')]) {
                        remote.user = userName
                        remote.identityFile = identity
                        // 登陆到阿里云镜像仓库
                        sshCommand remote: remote, command: &quot;docker login -u=${DOCKER_REGISTRY_USR} -p=${DOCKER_REGISTRY_PSW} ${REGISTRY_URL}&quot;
                        try {
                            // 删除已经存在的容器
                            sshCommand remote: remote, command: &quot;docker rm -f ${CONTAINER_NAME}&quot;
                        } catch (e) {
                            echo '容器不存在'
                        }
                        // 运行容器
                        sshCommand remote: remote, command: &quot;docker run --name ${CONTAINER_NAME} -p 8080:8080 -d ${IMAGE_NAME}&quot;
                    }
                }
            }
        }
    }
}

</code></pre>
<h2 id="集成-sonarqube">集成 Sonarqube</h2>
<p>参考：<a href="/post/38512795/">Maven 项目 Jenkins Pipeline 使用 Sonarqube</a></p>
]]></content>
    </entry>
</feed>